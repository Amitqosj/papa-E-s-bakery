# Papa E's Bakery — Render Web Service (Node)
# Serves the Vite production build from ./dist

services:
  - type: web
    name: papa-es-bakery
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
